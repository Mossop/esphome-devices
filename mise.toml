[tools]
"pipx:esphome" = { version = "latest", uvx_args = "--with pip" }

[tasks.setup]
run = "uv pip install -r requirements.txt"

[tasks.compile]
run = "esphome compile"

[tasks.run]
run = "esphome run"
